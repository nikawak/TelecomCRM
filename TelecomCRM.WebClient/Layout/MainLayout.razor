@inherits LayoutComponentBase

<div class="d-flex">
    <NavMenu />

    <!-- Сдвигаем контент, чтобы не налезал на фиксированный sidebar -->
    <div class="flex-grow-1" style="margin-left: 250px;">
        <header class="bg-primary text-white p-3">
            <h4 class="mb-0">Telecom CRM</h4>
        </header>

        <main class="p-4">
            @Body
        </main>
    </div>
</div>
